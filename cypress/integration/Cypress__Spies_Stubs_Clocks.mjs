// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Curry from "rescript/lib/es6/curry.js";
import * as Cypress$RescriptMonorepo from "../../src/bindings/Cypress.mjs";

Cypress$RescriptMonorepo.context("Spies, Stubs, and Clock", (function (param) {
        Cypress$RescriptMonorepo.it("cy.spy() - wrap a method in a spy", (function (param) {
                cy.visit("https://example.cypress.io/commands/spies-stubs-clocks", undefined);
                var obj = {
                  foo: (function (param) {
                      return "hello";
                    })
                };
                var spy = cy.spy(obj, "foo").as("anyArgs");
                var myStr = Curry._1(obj.foo, undefined);
                Cypress$RescriptMonorepo.expect(myStr).equal("hello");
                Cypress$RescriptMonorepo.expect(spy).to.be.called;
                
              }));
        Cypress$RescriptMonorepo.it("cy.spy() retries until assertions pass", (function (param) {
                cy.visit("https://example.cypress.io/commands/spies-stubs-clocks", undefined);
                var obj = {
                  foo: (function (x) {
                      console.log("obj.foo called with", x);
                      
                    })
                };
                cy.spy(obj, "foo").as("foo");
                setTimeout((function (param) {
                        return Curry._1(obj.foo, "first");
                      }), 500);
                setTimeout((function (param) {
                        return Curry._1(obj.foo, "second");
                      }), 2500);
                return Cypress$RescriptMonorepo.shouldHaveBeenCalledTwice(cy.get("@foo", undefined));
              }));
        Cypress$RescriptMonorepo.it("cy.stub() - create a stub and/or replace a function with stub", (function (param) {
                cy.visit("https://example.cypress.io/commands/spies-stubs-clocks", undefined);
                var obj = {
                  foo: (function (a, b) {
                      console.log("a", a, "b", b);
                      
                    })
                };
                var stub = cy.stub(obj, "foo").as("foo");
                Curry._2(obj.foo, "foo", "bar");
                Cypress$RescriptMonorepo.expect(stub).to.be.called;
                
              }));
        Cypress$RescriptMonorepo.it("cy.clock() - control time in the browser", (function (param) {
                var now = new Date(Date.UTC(2017, 2, 14)).getTime();
                cy.clock(now, undefined, undefined);
                cy.visit("https://example.cypress.io/commands/spies-stubs-clocks", undefined);
                return Cypress$RescriptMonorepo.shouldHaveText(cy.get("#clock-div", undefined).click(undefined), "1489449600");
              }));
        Cypress$RescriptMonorepo.it("cy.clock() - control time in the browser", (function (param) {
                var now = new Date(Date.UTC(2017, 2, 14)).getTime();
                cy.clock(now, undefined, Cypress$RescriptMonorepo.LocalStorageOptions.make(undefined, undefined));
                cy.visit("https://example.cypress.io/commands/spies-stubs-clocks", undefined);
                return Cypress$RescriptMonorepo.shouldHaveText(cy.get("#clock-div", undefined).click(undefined), "1489449600");
              }));
        return Cypress$RescriptMonorepo.it("cy.tick() - move time in the browser", (function (param) {
                      var now = new Date(Date.UTC(2017, 2, 14)).getTime();
                      cy.clock(now, undefined, undefined);
                      cy.visit("https://example.cypress.io/commands/spies-stubs-clocks", undefined);
                      Cypress$RescriptMonorepo.shouldHaveText(cy.get("#tick-div", undefined).click(undefined), "1489449600");
                      cy.tick(10000, undefined);
                      return Cypress$RescriptMonorepo.shouldHaveText(cy.get("#tick-div", undefined).click(undefined), "1489449610");
                    }));
      }));

export {
  
}
/*  Not a pure module */
