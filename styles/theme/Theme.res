// TODO: Write feature request for rescript-vscode - Let users decide what modules to expose in your API
module Screens = Theme__Screens

module Spacing = Theme__Spacing

module Proportions = Theme__Proportions

module Opacity = Theme__Opacity

module BorderRadius = Theme__BorderRadius

module BorderWidth = Theme__BorderWidth

module BoxShadow = Theme__BoxShadow

module MaxWidth = Theme__MaxWidth

module ZIndex = Theme__ZIndex

module Order = Theme__Order

module Duration = Theme__Duration

module TimingFunction = Theme__TimingFunction

module KeyFrames = Theme__KeyFrames

module Skew = Theme__Skew

module Scale = Theme__Scale

module Rotate = Theme__Rotate

module Colors = Theme__Colors

module FontSize = Theme__FontSize

module LetterSpacing = Theme__LetterSpacing

module LineHeight = Theme__LineHeight
